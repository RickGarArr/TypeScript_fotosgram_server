import { Request, Response } from "express";
import { ObjectId } from "mongoose";

export default function saveImages(req: Request, res: Response, idUsuario: ObjectId) {
    console.log("entre a la funcion");
    
}